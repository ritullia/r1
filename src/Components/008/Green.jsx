function Green({ green }) {
    return (
        <div
            className="mr-green"
            style={{
                backgroundColor: green,
                padding: '10px',
            }}
        ></div>
    );
}

export default Green;
